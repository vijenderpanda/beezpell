{
  "name": "beezpell",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install -w client -w server",
    "dev": "concurrently \"npm run dev -w client\" \"npm run dev -w server\"",
    "seed": "node server/db/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
